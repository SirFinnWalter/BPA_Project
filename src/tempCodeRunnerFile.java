
        // if (fillTileID > -1) {
        // Rectangle camera = renderer.getCamera();

        // for (int y = 0; y < camera.height; y++) {
        // for (int x = 0; x < camera.width; x++) {

        // }
        // }
        // }